{
  "version": 2,
  "builds": [{
    "src": "streamlit_app.py",
    "use": "@vercel/python",
    "config": {
      "pythonVersion": "3.10",
      "installCommand": "pip install --prefer-binary -r requirements.txt"
    }
  }]
}